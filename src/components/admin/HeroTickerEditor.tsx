"use client";
import type { TickerItem } from "@/types/site-config";
const input = "w-full rounded border border-neutral-800 bg-neutral-950 px-3 py-2 text-xs text-neutral-200 outline-none focus:border-white";
export function HeroTickerEditor({ items, onChange }: { items: TickerItem[]; onChange: (items: TickerItem[]) => void }) {
  const update = (id: string, key: keyof TickerItem, value: string) => onChange(items.map((item) => item.id === id ? { ...item, [key]: value } : item));
  return <section className="space-y-4 rounded-xl border border-neutral-800 bg-neutral-900 p-5"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-neutral-500">Ticker 2</p><h2 className="mt-1 text-lg font-bold uppercase">Hero mid-marquee</h2></div><button type="button" onClick={() => onChange([...items, { id: crypto.randomUUID(), text: "NEW ANNOUNCEMENT", icon: "✳", linkUrl: "" }])} className="rounded bg-white px-3 py-2 text-xs font-bold text-black">+ Add badge</button></div>{items.map((item, index) => <div className="grid gap-3 rounded-lg border border-neutral-800 bg-black p-3 md:grid-cols-[2rem_1fr_7rem_1fr_auto] md:items-end" key={item.id}><span className="font-mono text-xs text-neutral-500">{String(index + 1).padStart(2, "0")}</span><label className="text-[10px] uppercase tracking-widest text-neutral-500">Badge text<input className={input} value={item.text} onChange={(e) => update(item.id, "text", e.target.value)} /></label><label className="text-[10px] uppercase tracking-widest text-neutral-500">Icon<input className={input} value={item.icon || ""} onChange={(e) => update(item.id, "icon", e.target.value)} /></label><label className="text-[10px] uppercase tracking-widest text-neutral-500">Link URL<input className={input} value={item.linkUrl || ""} onChange={(e) => update(item.id, "linkUrl", e.target.value)} /></label><button type="button" onClick={() => onChange(items.filter((entry) => entry.id !== item.id))} className="px-2 py-2 text-xs text-red-400" aria-label={`Remove badge ${index + 1}`}>Remove</button></div>)}</section>;
}
