import Link from "next/link";
import { articles, events, magazine, radarProjects } from "@/lib/ia-content";
const rails = [{ href: "/ontheradar/articles", label: "Articles", count: articles.length, copy: "Shorter signals, updates, and quick features." }, { href: "/ontheradar/magazine", label: "Magazine", count: magazine.length, copy: "Long-form conversations and cultural stories." }, { href: "/ontheradar/projects", label: "Projects", count: radarProjects.length, copy: "The creative work built across the ecosystem." }, { href: "/ontheradar/events", label: "Events", count: events.length, copy: "Rooms, sessions, and nights to come." }];
export default function OnTheRadarPage() { return <div className="pt-14"><div className="px-4 pb-16 pt-20 md:px-8 md:pt-28"><p className="font-mono text-xs font-bold uppercase tracking-widest text-muted-foreground">(02 / On The Radar)</p><h1 className="mt-4 display text-[clamp(3rem,11vw,11rem)] leading-[.84]">Stories<br /><span className="text-flare">in motion.</span></h1></div><div className="grid border-t-2 border-ink md:grid-cols-2">{rails.map((rail, i) => <Link key={rail.href} href={rail.href} className={`group border-b-2 border-ink p-6 transition-colors hover:bg-flare md:p-10 ${i % 2 === 0 ? "md:border-r-2" : ""}`}><div className="flex justify-between font-mono text-xs font-bold uppercase tracking-widest"><span>{rail.count} entries</span><span>0{i + 1}</span></div><h2 className="mt-24 display text-5xl">{rail.label}<span className="text-flare group-hover:text-ink">.</span></h2><p className="mt-5 max-w-sm font-mono text-sm uppercase tracking-wide text-muted-foreground group-hover:text-ink">{rail.copy}</p></Link>)}</div></div>; }
